# Vetra UI - Environment Variables Example
# Copy this file to .env.local for local development

# Next.js Configuration
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME=Vetra UI

# Runtime Mode
# Use :static (default) for static export via out/
# Switch to :dynamic for SSR / Node runtime
NEXT_RUNTIME_MODE=:static

# Analytics (Optional)
# NEXT_PUBLIC_GA_ID=
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=

# Contact Form (Optional)
# NEXT_PUBLIC_FORM_API_ENDPOINT=

# Feature Flags (Optional)
# NEXT_PUBLIC_ENABLE_DARK_MODE=true
